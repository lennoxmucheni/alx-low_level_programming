#include <stdio.h>
/**
* main - Entry point
* Description: 'Display single digit nos of base 10 using putchar'
* Return: Always 0 (Success)
*/
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
	{
		putchar((char(number);
	}
	putchar("\n");
	return (0);
}
